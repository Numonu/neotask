import { produce } from "immer";

export const dataReducer = produce((draft , action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
})